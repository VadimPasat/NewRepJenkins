  
pipeline {
   agent any
   stages {
       stage('read') {
           steps {
               script {
				echo 'Starting test with Taurus'
				bzt "stuff.yml -report -o settings.artifacts-dir=artifacts"
				
               }
           }
       }
   }
}
